{
  "name": "js-express-task-1",
  "version": "1.0.0",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "express": "^4.18.2",
    "sequelize": "^6.31.0",
    "sequelize-joi": "^2.0.1"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "pg": "^8.10.0",
    "sequelize-cli": "^6.6.0",
    "supertest": "^6.3.3"
  },
  "scripts": {
    "test": "set NODE_ENV=test&&npx sequelize-cli db:migrate && jest && set NODE_ENV=test&&npx sequelize-cli db:migrate:undo:all ",
    "starttest": "set NODE_ENV=test && node app.js"
  }
}
